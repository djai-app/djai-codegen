package {{basePackage}}.controller

import {{basePackage}}.domain.BaseResource
import org.springframework.data.domain.Page
import org.springframework.data.domain.Pageable
import org.springframework.http.ResponseEntity
{{#entityHasUUID}}
import java.util.UUID
{{/entityHasUUID}}

interface CommonController<E : BaseResource> {
    fun create(domain: E): ResponseEntity<E>
    fun delete(id: {{entityIdType}}): ResponseEntity<E>
    fun getById(id: {{entityIdType}}): ResponseEntity<E>
    fun getAll(search: String?, pageable: Pageable): ResponseEntity<Page<E>>
    fun update(id: {{entityIdType}}, domain: E): ResponseEntity<E>
    fun modify(id: {{entityIdType}}, domain: E): ResponseEntity<E>
    fun saveAll(domains: List<E>): List<E>
    fun deleteAll(ids: List<{{entityIdType}}>): List<E>
}
